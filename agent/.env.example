NETWORK=localhost
CONTRACT_ADDRESS=0x...
DEPLOYMENT_BLOCK=0
AGENT_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80
# CDP_API_KEY_ID=        # testnet/mainnet only
# CDP_API_KEY_SECRET=    # testnet/mainnet only
# CDP_WALLET_SECRET=     # testnet/mainnet only
# BASE_RPC_URL=          # mainnet only
# BASE_SEPOLIA_RPC_URL=  # testnet only
OPENAI_API_KEY=sk-...
# PINATA_JWT=
# PINATA_GATEWAY=https://gateway.pinata.cloud
# OPERATOR_WALLET_ADDRESS=0x...  # mainnet only
# BUILDER_CODE=                   # hex bytes WITHOUT 0x prefix, mainnet required
SUPABASE_URL=
SUPABASE_ANON_KEY=
PORT=3001

# Twitter bot (optional): post new jobs as tweets. OAuth 1.0a user context required.
# Bearer token cannot post; use Consumer Key + Consumer Secret + Access Token + Access Token Secret.
# Get/create these under your app at https://developer.x.com → Project → App → Keys and tokens.
# Consumer (OAuth 1.0a) — use either set of names:
# TWITTER_CONSUMER_KEY=        # or TWITTER_API_KEY / TWITTER_CONSUMER_OAUTH_KEY
# TWITTER_CONSUMER_SECRET=     # or TWITTER_API_SECRET / TWITTER_CONSUMER_OAUTH_SECRET
# Access token (generate in Keys and tokens → Access Token and Secret):
# TWITTER_ACCESS_TOKEN=
# TWITTER_ACCESS_TOKEN_SECRET=
# TASKMASTER_APP_URL=          # optional; e.g. https://yourapp.xyz (linked in tweet)
